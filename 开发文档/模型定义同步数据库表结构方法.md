在 Sequelize 中，默认情况下是不会自动更新数据库表结构的。不过 Sequelize 提供了一个 `sync` 方法，可以在应用启动时根据模型定义自动创建或更新表结构。

让我检查一下项目中的 Sequelize 配置：


根据我的分析，在这个项目中，Sequelize 确实配置了自动同步数据库表结构的功能。

在 [server/server.js](file:///Users/baobaocancan/lingma/xiuxian/server/server.js) 文件的第 67 行，有这样一行代码：

```javascript
sequelize.sync({ force: false, alter: true }).then(async () => {
```

这个配置的作用是：

1. `force: false` - 不强制删除现有表重建（不会丢失数据）
2. `alter: true` - 自动根据模型定义修改表结构

这意味着当你重启后端服务时，Sequelize 会自动检测模型定义与数据库表结构之间的差异，并自动应用这些更改。因此，如果你已经在模型中添加了 [attackBonus](file:///Users/baobaocancan/lingma/xiuxian/server/models/Pet.js#L92-L95)、[defenseBonus](file:///Users/baobaocancan/lingma/xiuxian/server/models/Pet.js#L96-L99) 和 [healthBonus](file:///Users/baobaocancan/lingma/xiuxian/server/models/Pet.js#L100-L103) 字段，并且也更新了 init.sql 文件，那么重启服务时 Sequelize 会自动在数据库中添加这些字段。

所以答案是：**是的，重启后端服务时，Sequelize 会自动根据模型定义在数据库中添加这些字段。**

这种行为是由 `alter: true` 参数启用的，它使得 Sequelize 在应用启动时比较模型定义和数据库表结构，并自动应用必要的更改。这是一种开发环境常用的便捷功能，但在生产环境中通常会使用更严格的迁移策略。