1、前端显示pinia stores数据-后端api返回字段笔记
前端需要显示label="灵力获取速率">{{ playerInfoStore.spiritGainRate }} / 秒
a、先在playerInfoStores增加spiritGainRate字段
b、后端接口返回的数据取用data.gainAmount赋值playerInfoStore.spiritGainRate
c、前端展示
 <n-descriptions-item label="灵力获取速率">{{ playerInfoStore.spiritGainRate }} / 秒</n-descriptions-item>
        