# 秘境功能代码清单

## 📁 文件结构

```
/src/
├── views/
│   └── Dungeon.vue              ⭐ 主界面组件 (699行)
├── plugins/
│   ├── dungeon.js               ⭐ 增益配置与选项生成 (285行)
│   ├── combat.js                ⭐ 战斗系统核心逻辑 (361行)
│   └── dungeonBuffs.js          ⭐ 增益管理器 (53行)
└── workers/
    └── exploration.js           (需要创建或参考)
```

---

## 📋 模块详细清单

### 1️⃣ Dungeon.vue - 主界面 (699行)

**文件路径**: `/src/views/Dungeon.vue`

#### 核心组成

| 部分 | 行号 | 功能 |
|------|------|------|
| Template | 1-287 | UI布局 (难度选择、增益卡、战斗界面、属性面板、日志) |
| Script | 290-699 | 逻辑处理 |

#### Script部分详解

**导入与初始化** (290-312)
```javascript
import { usePlayerInfoStore, ... } from '../stores'
import { useMessage } from 'naive-ui'
import LogPanel from '../components/LogPanel.vue'
import { difficultyModifiers, roguelikeOptions, getRandomOptions } from '../plugins/dungeon'

// 初始化所有store实例
const playerInfoStore = usePlayerInfoStore()
// ... 其他store
```

**核心数据结构** (314-346)

| 变量名 | 类型 | 说明 |
|--------|------|------|
| `showBattleLog` | ref(false) | 是否显示战斗日志 |
| `battleLogs` | ref([]) | 战斗日志数组 |
| `currentEnemy` | ref(null) | 当前敌人对象 |
| `isInBattle` | ref(false) | 战斗状态 |
| `dungeonWorker` | ref(null) | Web Worker实例 |
| `refreshNumber` | ref(3) | 剩余刷新次数 |
| `dungeonState` | ref({...}) | 秘境全局状态 |
| `combatLog` | ref([]) | 实时战斗日志 |

**秘境状态结构** (340-346)
```javascript
const dungeonState = {
  floor: 1,              // 当前层数
  inCombat: false,       // 是否战斗中
  showingOptions: false, // 是否显示增益选项
  currentOptions: [],    // 当前3个增益选项
  combatManager: null    // 战斗管理器实例
}
```

**计算属性** (349-432)

1. **playerStats** (349-432)
   - 计算玩家总属性
   - 包含基础属性 + 装备加成(=0) + 灵宠加成(=0)
   - 返回23个属性字段

2. **enemyStats** (435-470)
   - 计算敌人属性
   - 基于层数、难度、玩家属性
   - 返回23个属性字段

**关键方法列表**

| 方法名 | 行号 | 功能 |
|--------|------|------|
| `startDungeon()` | 599-604 | 初始化秘境 |
| `generateOptions()` | 607-609 | 生成3个增益选项 |
| `selectOption(option)` | 620-672 | 选择增益并进入战斗 |
| `handleRefreshOptions()` | 612-617 | 刷新选项 |
| `addBattleLog(message)` | 473-482 | 添加战斗日志 |
| `startBattle(enemy)` | 485-516 | 启动Web Worker战斗 |
| `finishBattle(result)` | 519-550 | 战斗结束处理 |
| `gainBattleReward(rewards)` | 553-584 | 处理战斗奖励 |
| `flee()` | 587-596 | 逃跑 |
| `getOptionColor(type)` | 675-686 | 获取增益品质颜色 |
| `infoCliclk(type)` | 689-692 | 点击查看详细属性 |

**Web Worker通信** (493-516)
```javascript
dungeonWorker.value = new Worker(new URL('../workers/exploration.js', import.meta.url))
dungeonWorker.value.onmessage = (e) => {
  if (type === 'battle_update') { ... }
  else if (type === 'battle_end') { ... }
}
dungeonWorker.value.postMessage({
  type: 'start_battle',
  player: playerStats.value,
  enemy: enemyStats.value
})
```

**样式** (701-926)
- 增益卡片样式 (.option-card)
- 战斗场景布局 (.combat-scene)
- 角色动画 (@keyframes attack, hurt, enemy-attack)
- 攻击特效动画

---

### 2️⃣ dungeon.js - 增益配置 (285行)

**文件路径**: `/src/plugins/dungeon.js`

#### 数据结构

**1. 难度修饰符** (2-7)
```javascript
const difficultyModifiers = {
  easy:   { healthMod: 0.8,  damageMod: 0.8,  rewardMod: 0.8 },
  normal: { healthMod: 1.0,  damageMod: 1.0,  rewardMod: 1.0 },
  hard:   { healthMod: 1.2,  damageMod: 1.2,  rewardMod: 1.5 },
  expert: { healthMod: 1.5,  damageMod: 1.5,  rewardMod: 2.0 }
}
```

**2. 增益选项池** (10-227)

按品质分类:
- **common** (11-93): 8个普通增益
- **rare** (94-156): 6个稀有增益
- **epic** (157-227): 6个史诗增益

每个增益对象结构:
```javascript
{
  id: string,              // 唯一ID
  name: string,            // 增益名称
  description: string,     // 增益描述
  effect: function(player) // 效果函数 (修改playerStats)
}
```

**增益详细列表**

普通品质:
1. `heal` - 气血增加 (health × 1.1)
2. `small_buff` - 小幅强化 (finalDamageBoost += 0.1)
3. `defense_boost` - 铁壁 (defense × 1.2)
4. `speed_boost` - 疾风 (speed × 1.15)
5. `crit_boost` - 会心 (critRate += 0.15)
6. `dodge_boost` - 轻身 (dodgeRate += 0.15)
7. `vampire_boost` - 吸血 (vampireRate += 0.1)
8. `combat_boost` - 战意 (combatBoost += 0.1)

稀有品质:
1. `defense_master` - 防御大师
2. `crit_mastery` - 会心精通
3. `dodge_master` - 无影
4. `combo_master` - 连击精通
5. `vampire_master` - 血魔
6. `stun_master` - 震慑

史诗品质:
1. `ultimate_power` - 极限突破 (+50%战斗属性)
2. `divine_protection` - 天道庇护 (+30%减伤)
3. `combat_master` - 战斗大师 (+25%属性和抗性)
4. `immortal_body` - 不朽之躯 (血量×2, 减伤+20%)
5. `celestial_might` - 天人合一 (属性+40%, 血量×1.5)
6. `battle_sage_supreme` - 战圣至尊 (暴击+40%, 爆伤+80%, 伤害+20%)

**3. 选项生成函数** (230-282)

```javascript
const getRandomOptions = (floor) => {
  // 1. 根据floor计算概率
  //    floor % 10 === 0: 提高epic概率到20%
  //    floor % 5 === 0: 提高rare概率到35%
  
  // 2. 生成3个选项
  //    - 每个独立随机决定品质
  //    - 记录已使用ID避免重复
  //    - 品质池用尽时从其他池选择
  
  // 3. 返回selected数组
  return selected
}
```

核心逻辑 (250-280)
```javascript
while (selected.length < 3) {
  const rand = Math.random()
  let pool = 'common'
  
  if (rand < epicChance) pool = 'epic'
  else if (rand < epicChance + rareChance) pool = 'rare'
  
  const options = roguelikeOptions[pool].filter(opt => !usedIds.has(opt.id))
  
  if (options.length > 0) {
    // 从池中随机选择
    const option = options[Math.floor(Math.random() * options.length)]
    option.type = pool
    selected.push(option)
    usedIds.add(option.id)
  } else {
    // 从所有未使用选项中选择
  }
}
```

---

### 3️⃣ combat.js - 战斗系统 (361行)

**文件路径**: `/src/plugins/combat.js`

#### 枚举定义

**CombatState** (2-7)
```javascript
{
  READY: 'ready',
  IN_PROGRESS: 'in_progress',
  VICTORY: 'victory',
  DEFEAT: 'defeat'
}
```

**CombatType** (9-14)
```javascript
{
  NORMAL: 'normal',
  BOSS: 'boss',
  ELITE: 'elite'
}
```

#### 核心类

**1. CombatStats** (17-106)

构造函数初始化23个属性:

基础属性:
```javascript
this.health = 100
this.maxHealth = 100
this.damage = 10
this.defense = 5
this.speed = 10
```

战斗属性 (%)
```javascript
this.critRate = 0.05
this.comboRate = 0
this.counterRate = 0
this.stunRate = 0
this.dodgeRate = 0.05
this.vampireRate = 0
```

战斗抗性 (%)
```javascript
this.critResist = 0
this.comboResist = 0
this.counterResist = 0
this.stunResist = 0
this.dodgeResist = 0
this.vampireResist = 0
```

特殊属性 (%)
```javascript
this.healBoost = 0
this.critDamageBoost = 0.5
this.critDamageReduce = 0
this.finalDamageBoost = 0
this.finalDamageReduce = 0
this.combatBoost = 0
this.resistanceBoost = 0
```

方法:
- `calculateDamage(target)` (49-91) - 伤害计算
- `calculateDamageReduction(incomingDamage, attackerStats)` (93-105) - 伤害减免

**2. CombatEntity** (109-170)

属性:
```javascript
this.name = name
this.level = level
this.realm = realm
this.stats = new CombatStats(stats)
this.currentHealth = this.stats.maxHealth
this.effects = [] // 状态效果
```

方法:
- `takeDamage(amount, source)` (124-149)
- `heal(amount)` (151-155)
- `addEffect(effect)` (157-160)
- `removeEffect(effectId)` (162-169)

**3. CombatManager** (172-286)

构造函数:
```javascript
this.player = player
this.enemy = enemy
this.type = type
this.state = CombatState.READY
this.round = 0
this.maxRounds = 100
this.log = []
```

方法:

| 方法名 | 行号 | 功能 |
|--------|------|------|
| `start()` | 183-186 | 开始战斗 |
| `executeTurn()` | 188-281 | 执行一个回合 |
| `getCombatLog()` | 283-285 | 获取战斗日志 |

**executeTurn() 详解** (188-281)

流程:
1. 增加回合数
2. 检查最大回合数 (超过100 → 失败)
3. 比较双方速度决定先后手
4. 第一攻击者攻击:
   - 计算伤害 (calculateDamage)
   - 第二攻击者受伤 (takeDamage)
   - 记录日志
   - 检查是否死亡
5. 第二攻击者攻击 (如果未被眩晕):
   - 检查反击触发
   - 计算伤害
   - 第一攻击者受伤
   - 记录日志
   - 检查是否死亡

**4. generateEnemy() 函数** (289-360)

```javascript
function generateEnemy(level, type = CombatType.NORMAL, difficulty = 1)
```

参数:
- `level` - 敌人等级
- `type` - 敌人类型 (NORMAL/ELITE/BOSS)
- `difficulty` - 难度系数

流程:
1. 计算基础属性
2. 根据类型调整属性 (倍数)
3. 根据type选择敌人名称
4. 返回 CombatEntity 实例

敌人名称池:
```javascript
const normalNames = ['野狼', '山猪', ...]
const eliteNames = ['赤焰虎', '玄冰蟒', ...]
const bossNames = ['九尾天狐', '万年龙蟒', ...]
```

#### 导出

```javascript
export {
  CombatState,
  CombatType,
  CombatStats,
  CombatEntity,
  CombatManager,
  generateEnemy
}
```

---

### 4️⃣ dungeonBuffs.js - 增益管理器 (53行)

**文件路径**: `/src/plugins/dungeonBuffs.js`

```javascript
const dungeonBuffs = {
  activeBuffs: [],  // 存储应用的增益
  
  apply(player, option) {
    // 1. 添加到activeBuffs列表
    // 2. 执行effect函数
  },
  
  clear(player) {
    // 1. 重置所有可能被修改的属性为基础值
    // 2. 清空activeBuffs列表
  },
  
  getActiveBuffs() {
    return this.activeBuffs
  }
}

export default dungeonBuffs
```

#### 重置的属性

baseAttributes:
- attack, defense, speed, health

combatAttributes:
- critRate, comboRate, counterRate, stunRate, dodgeRate, vampireRate

specialAttributes:
- healBoost, critDamageBoost, critDamageReduce, finalDamageBoost, finalDamageReduce, combatBoost, resistanceBoost

---

## 🔗 组件交互关系

```
Dungeon.vue
    │
    ├─ 导入 → dungeon.js
    │         getRandomOptions()
    │         roguelikeOptions
    │         difficultyModifiers
    │
    ├─ 导入 → combat.js
    │         CombatManager
    │         generateEnemy()
    │
    ├─ 导入 → dungeonBuffs.js (可选)
    │         apply()
    │         clear()
    │
    └─ 使用 → Web Worker
              exploration.js
              (战斗计算)
```

---

## 📊 代码统计

| 文件 | 行数 | 功能 |
|------|------|------|
| Dungeon.vue | 926 | UI + 状态管理 |
| dungeon.js | 285 | 增益配置 |
| combat.js | 361 | 战斗逻辑 |
| dungeonBuffs.js | 53 | 增益管理(可选) |
| **总计** | **1625** | |

---

## 🎯 关键数据流

```
startDungeon()
  ↓
generateOptions() ← dungeon.js
  ↓
selectOption(option)
  ├─ option.effect(playerStats) ← 应用增益
  └─ startBattle()
      ↓
      postMessage({type:'start_battle',...}) → Worker
          ↓
          CombatManager.executeTurn() ← combat.js
          [循环100次或直到结束]
          ↓
      onmessage({type:'battle_end'}) ← Worker
          ↓
      finishBattle(result)
      ├─ 胜利 → 下一层 (goto generateOptions)
      └─ 失败 → 秘境结束
```

---

## ✅ 功能完整性检查

- [x] 难度选择 (4个难度)
- [x] 增益选项生成 (20个增益, 3个/轮)
- [x] 增益效果应用
- [x] 增益刷新 (最多3次)
- [x] 战斗系统 (100+ 行代码实现完整机制)
- [x] 伤害计算 (暴击/连击/吸血/眩晕/反击)
- [x] 敌人生成 (基于层数+难度+类型)
- [x] Web Worker通信
- [x] 战斗动画 (血条/特效)
- [x] 战斗日志
- [x] 数据统计 (击杀数/死亡数)
- [x] 奖励系统 (灵石/灵草/装备/灵宠)

---

## 🚀 使用方式

### 1. 在主应用中导入
```javascript
import Dungeon from '@/views/Dungeon.vue'
// 在路由中配置
```

### 2. 初始化增益效果
```javascript
import { getRandomOptions } from '@/plugins/dungeon'
const options = getRandomOptions(floor)
```

### 3. 创建战斗实例
```javascript
import { CombatManager, CombatEntity, generateEnemy } from '@/plugins/combat'

const player = new CombatEntity('玩家', level, playerStats)
const enemy = generateEnemy(floor, CombatType.NORMAL, difficulty)
const manager = new CombatManager(player, enemy)
manager.start()
```

### 4. 在Worker中执行战斗
```javascript
const result = manager.executeTurn()
while (manager.state === CombatState.IN_PROGRESS) {
  manager.executeTurn()
}
```

---

## 📝 注意事项

1. **Web Worker路径**: 代码引用 `exploration.js` 作为Worker脚本
   - 需要确保该文件存在或创建专属的 `dungeon-worker.js`
   
2. **属性覆盖**: 增益的effect函数直接修改playerStats
   - 确保playerStats.stats存在
   - 避免undefined访问
   
3. **难度系数**: `statsStore.dungeonDifficulty` 应该是字符串
   - 'easy' | 'normal' | 'hard' | 'expert'
   - 需要通过difficultyModifiers映射
   
4. **RNG分布**: Math.random()完全在客户端
   - 可被修改，建议后端集成
   - 增益选择不公平
   
5. **性能**: 单个战斗回合涉及多次属性访问
   - Web Worker隔离计算，不影响UI
   - 但大量计算(100回合)可能有延迟

---

## 🔄 改进建议

### 短期 (优化现有代码)
- [ ] 添加属性上限限制
- [ ] 修复难度系数映射
- [ ] 添加负面增益
- [ ] 优化敌人属性增长曲线

### 中期 (功能扩展)
- [ ] 添加Boss独特技能
- [ ] 实现增益选择困难 (舍弃机制)
- [ ] 添加特殊事件 (随机商人、休息点)
- [ ] 实现增益组合奖励

### 长期 (后端集成)
- [ ] 秘境成绩保存 (数据库)
- [ ] RNG后端化 (种子同步)
- [ ] 战斗计算后端化 (安全性)
- [ ] 排行榜功能
- [ ] 成就系统

